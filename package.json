{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "pulp build && rimraf docs/Control && rimraf docs/Data && pulp docs"
  },
  "devDependencies": {
    "pulp": "^4.0.1",
    "rimraf": "^2.4.1"
  }
}
